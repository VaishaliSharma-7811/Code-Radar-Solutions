#include<stdio.h>

int main(){
    int a;
    scanf("%d", &a);
    for(int n = 1; n <= a; n++){
        printf("%d", n);
        if (i < a){
            printf("");
        }
    }
    return 0;
}